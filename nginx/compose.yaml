networks:
  traefik:
    name: treafik_default
    external: true

services:
  whoami:
    image: "nginx"
    networks:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.nginx.rule=Host(`nginx.localhost`)"
      - "traefik.http.routers.nginx.entrypoints=web"